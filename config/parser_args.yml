positional:
  # name  nargs           default       help         
  source: ["*",           ~,            "parse source / list of sources (enter space seperated if you want to define multiple sources)" ]

flags:
  # short flag            choices                                     nargs       action      default   type        help
  a:      [approach,      [us, ex, pd, pl, gt],                       ~,          ~,          gt,       str,        "choose method / approach of decoding BUFR files (default=gt)"        ]
  A:      [aggregated,    ~,                                          ~,          store_true, ~,        ~,          "derive only aggregated elements (when called after aggregate_obs.py" ]
  b:      [bare,          ~,                                          ~,          store_true, ~,        ~,          "do not actually run chain scripts, only print the bare commands"     ]
  B:      [bad_obs,       ~,                                          ~,          store_true, ~,        ~,          "handle bad obs as well (meaning delete them in emtpy_obs.py script)" ]
  c:      [clusters,      ~,                                          ~,          ~,          ~,        split_set,  "station clusters to consider, comma seperated"                       ]
#  c:      [clusters,      ~,                                          ~,          split_args, ~,        set,        "station clusters to consider, comma seperated"                       ]
  C:      [config_dir,    ~,                                          ~,          ~,          ~,        str,        "set custom path of the config directory (default=config)"            ]
  d:      [debug,         ~,                                          ~,          store_true, ~,        ~,          "enable debugging (using pdb and debug prints)"                       ]
  D:      [divider,       ~,                                          ~,          ~,          ;,        str,        "divider / seperator character for the file argument (default=;)"     ] 
  e:      [export,        ~,                                          ~,          store_true, ~,        ~,          "export data to legacy CSV format (generated by Metwatch)"            ]
  E:      [extra,         ~,                                          ~,          ~,          extra,    str,        "name of source for extra files (provided by -f or -F arguments)"     ]
  f:      [file,          ~,                                          ~,          ~,          ~,        str,        "parse single BUFR file, will be handled as source=extra by default"  ]
  F:      [files,         ~,                                          ~,          ~,          ~,        str,        "parse one or more BUFR files; define seperator with --divider option"]
#  F:      [files,         ~,                                          ~,          ~,          ~,        split_tuple,"parse one or more BUFR files; define seperator with --divider option"]
#  F:      [files,         ~,                                          ~,          split_args, ~,        tuple,      "parse one or more BUFR files; define seperator with --divider option"]
  i:      [pid_file,      ~,                                          ~,          store_true, ~,        ~,          "use a PID file to check whether script is running"                   ]
  j:      [jump_checks,   ~,                                          ~,          store_true, ~,        ~,          "check whether the change between 2 time steps exceeds allowed values"]
  H:      [how,           [sorted,reversed],                          ~,          ~,          sorted,   ToEval,     "how to sort? sorting algorithm to be used by -s/--sort_files option" ]
  l:      [log_level,     [CRITICAL,ERROR,WARNING,INFO,DEBUG,NOTSET], ~,          ~,          NOTSET,   str,        "set logging level"                                                   ]
  L:      [legacy_output, ~,                                          ~,          ~,          ~,        str,        "define output directory for old system's CSV format"                 ]
  k:      [known_stations,~,                                          ~,          ~,          ~,        split_set,  "only extract obs from these stations (comma-seperated list)"         ]
  m:      [max_retries,   ~,                                          ~,          ~,          ~,        int,        "maximum attemps when communicating with station databases"           ]
  M:      [mode,          [oper,dev,test],                            ~,          ~,          dev,      str,        "set operation mode for the script (default=dev)"                     ]
  n:      [max_files,     ~,                                          ~,          ~,          ~,        int,        "maximum number of files to parse (per source)"                       ]
  N:      [save_null,     ~,                                          ~,          store_true, ~,        ~,          "save non-existing / empty observations as NULL values in databases"  ]
  o:      [timeout,       ~,                                          ~,          ~,          ~,        int,        "timeout in seconds when communicating with station databases"        ]
  O:      [output,        ~,                                          ~,          ~,          ~,        str,        "define output directory where the station databases will be saved"   ]
    #p:      [profiler,      ~,                                          ~,          ~,          ~,        str,        "enable profiler of your choice (default=None)"                       ]
  p:      [parse,         ~,                                          ~,          store_true, ~,        ~,          "parse the files right after downloading them, using BUFR decoder"    ]
  P:      [processes,     ~,                                          ~,          ~,          ~,        int,        "number of processes to use (enables multiprocessing mode if set)"    ]
  r:      [redo,          ~,                                          ~,          store_true, ~,        ~,          "decode_bufr again even if already processed / reduce or export again"]
  R:      [restart,       ~,                                          ~,          ~,          ~,        int,        "only parse all files with status=locked_{restart} (R=PID usually)"   ]
  s:      [sort_files,    ~,                                          ~,          store_true, ~,        ~,          "sort files alpha-numeric before parsing / sort legacy output files"  ]
  S:      [stage,         [raw,forge,bad,final],                      ~,          ~,          forge,    str,        "name of stage within staging chain processing (default=forge)"       ]
  t:      [traceback,     ~,                                          ~,          store_true, ~,        ~,          "enable traceback debugging prints (using the traceback module)"      ]
  T:      [translation,   ~,                                          ~,          ~,          ~,        str,        "name of custom (bufr) translation file"                              ]
  u:      [update,        ~,                                          ~,          store_true, ~,        ~,          "force already existing values to be updated during conflict handling"]
  v:      [verbose,       ~,                                          ~,          store_true, ~,        ~,          "show more detailed output"                                           ]
  V:      [values,        [10min, 30min, 1h, 3h, 6h],                 ~,          ~,          1h,       str,        "frequency of values for export in legacy / Metwatch CSV format"      ]
  w:      [no_warnings,   ~,                                          ~,          store_true, ~,        ~,          "show no warnings, force-suppress them all regardless of level"       ]
  x:      [not_exported,  ~,                                          ~,          store_true, ~,        ~,          "do not mark obs as exported in final databases after completion"     ]
