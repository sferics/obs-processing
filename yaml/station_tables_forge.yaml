obs:
  datetime: datetime NOT NULL
  duration: text
  element:  text NOT NULL
  value:    text NOT NULL
  UNIQUE:   (datetime, duration, element)

obs_bad:
  datetime: datetime NOT NULL
  duration: text
  element:  text NOT NULL
  value:    text NOT NULL
  reason:   text NOT NULL
  UNIQUE:   (datetime, duration, element)
